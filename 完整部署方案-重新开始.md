# 完整部署方案 - GitHub Pages + Vercel + MongoDB Atlas

## 📋 方案概述

本方案采用以下技术栈：
- **前端**：GitHub Pages（免费静态网站托管）
- **后端**：Vercel（免费Serverless函数）
- **数据库**：MongoDB Atlas（免费云数据库）

**优势**：
- ✅ 完全免费
- ✅ 自动HTTPS
- ✅ 全球CDN加速
- ✅ 自动部署（代码推送后自动更新）

---

## 🎯 部署前准备清单

在开始部署前，确保您已准备好：

- [ ] GitHub账号（[注册地址](https://github.com/signup)）
- [ ] Vercel账号（可使用GitHub账号登录，[注册地址](https://vercel.com/signup)）
- [ ] MongoDB Atlas账号（[注册地址](https://www.mongodb.com/cloud/atlas/register)）
- [ ] 项目代码已准备好

---

## 📚 第一部分：配置 MongoDB Atlas 云数据库

### 步骤 1.1：注册 MongoDB Atlas

1. 访问：https://www.mongodb.com/cloud/atlas/register
2. 使用邮箱注册账号（支持Gmail、GitHub快速注册）
3. 完成邮箱验证

### 步骤 1.2：创建免费集群

1. 登录后，点击 **"Build a Database"**
2. 选择 **FREE (M0)** 免费套餐
3. 选择云服务商和区域（建议选择 **Asia Pacific (Singapore)** 或离您最近的区域）
4. 集群名称保持默认 `Cluster0` 或自定义
5. 点击 **"Create Cluster"**
6. 等待 3-5 分钟，集群创建完成

### 步骤 1.3：配置数据库用户

1. 在安全设置中找到 **"Database Access"**
2. 点击 **"Add New Database User"**
3. 认证方式选择 **"Password"**
4. 输入用户名（如：`transguyane_admin`）
5. 输入强密码（至少8位，包含大小写字母、数字和特殊字符）
6. **重要**：请妥善保存用户名和密码，后续会用到
7. 用户权限选择：**"Read and write to any database"**
8. 点击 **"Add User"**

### 步骤 1.4：配置网络访问

1. 在左侧菜单找到 **"Network Access"**
2. 点击 **"Add IP Address"**
3. 为了方便开发和生产部署，选择：
   - **"Allow Access from Anywhere"** (0.0.0.0/0)
   - 或点击 **"Add Current IP Address"** 添加当前IP
   - 再添加 Vercel 的 IP（可选，建议允许所有IP）
4. 点击 **"Confirm"**

### 步骤 1.5：获取连接字符串

1. 在集群页面，点击 **"Connect"** 按钮
2. 选择 **"Connect your application"**
3. 选择驱动：**"Node.js"**，版本选择 **"5.5 or later"**
4. 复制连接字符串，格式如下：
   ```
   mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
5. **重要修改**：
   - 将 `<username>` 替换为您创建的数据库用户名（如：`transguyane_admin`）
   - 将 `<password>` 替换为您创建的数据库密码
   - 在 `mongodb.net/` 后面添加数据库名称：`transguyane`
   
   最终格式示例：
   ```
   mongodb+srv://transguyane_admin:YourPassword123!@cluster0.xxxxx.mongodb.net/transguyane?retryWrites=true&w=majority
   ```
6. **保存这个连接字符串**，后续在Vercel中配置时会用到

### 步骤 1.6：生成 JWT Secret

JWT Secret用于用户认证加密，需要至少32个字符的随机字符串。

**生成方法**：
- 可以使用在线工具：https://generate-secret.vercel.app/32
- 或使用命令：`node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
- 或手动创建一个至少32位的随机字符串

**保存这个 JWT Secret**，后续在Vercel中配置时会用到。

---

## 🚀 第二部分：部署后端到 Vercel

### 步骤 2.1：注册 Vercel 账号

1. 访问：https://vercel.com/signup
2. 点击 **"Continue with GitHub"**（推荐，便于后续集成）
3. 授权GitHub账号访问Vercel

### 步骤 2.2：准备项目代码

确保项目已推送到GitHub（如果还没有，先完成第三部分的步骤3.1-3.3）：

```bash
# 如果项目还没有推送到GitHub，先执行：
cd d:\【网站设计】\TransGuyane
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/TransGuyane.git
git push -u origin main
```

### 步骤 2.3：在 Vercel 中导入项目

1. 登录 Vercel 控制台：https://vercel.com/dashboard
2. 点击 **"Add New..."** → **"Project"**
3. 在 **"Import Git Repository"** 中找到您的 GitHub 仓库 `TransGuyane`
4. 如果没有看到，点击 **"Adjust GitHub App Permissions"** 授权访问您的仓库
5. 选择仓库后，点击 **"Import"**

### 步骤 2.4：配置项目设置

在项目配置页面：

1. **Project Name**：可以保持默认或自定义（如：`transguyane-backend`）
2. **Framework Preset**：选择 **"Other"**
3. **Root Directory**：点击 **"Edit"**，输入 `backend`
4. **Build Command**：留空（Vercel会自动处理）
5. **Output Directory**：留空
6. **Install Command**：留空（默认使用 `npm install`）
7. **Environment Variables**：**不要在这里添加**，我们在下一步单独配置

### 步骤 2.5：配置环境变量

在项目配置页面，点击 **"Environment Variables"**，添加以下变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `MONGODB_URI` | `mongodb+srv://用户名:密码@cluster0.xxxxx.mongodb.net/transguyane?retryWrites=true&w=majority` | 替换为您在步骤1.5中获取的连接字符串 |
| `JWT_SECRET` | `您的32位以上随机字符串` | 替换为您在步骤1.6中生成的JWT Secret |
| `NODE_ENV` | `production` | 固定值 |
| `FRONTEND_URL` | `https://您的GitHub用户名.github.io` | 稍后更新为实际的前端地址 |

**重要提示**：
- 如果密码包含特殊字符（如 `@`、`:`、`/`、`?`、`#`），需要进行URL编码：
  - `@` → `%40`
  - `:` → `%3A`
  - `/` → `%2F`
  - `?` → `%3F`
  - `#` → `%23`
  - `&` → `%26`
  - ` ` (空格) → `%20`

### 步骤 2.6：部署项目

1. 确认所有环境变量已添加
2. 点击 **"Deploy"** 按钮
3. 等待部署完成（通常需要1-3分钟）
4. 部署成功后，Vercel会提供一个URL，格式如：`https://transguyane-backend.vercel.app`
5. **复制并保存这个URL**，这就是您的后端API地址

### 步骤 2.7：验证后端部署

1. 访问后端健康检查接口：
   ```
   https://您的项目名.vercel.app/api/health
   ```
   应该返回：
   ```json
   {"status":"ok","message":"服务器运行正常"}
   ```

2. 访问API根路径：
   ```
   https://您的项目名.vercel.app/api
   ```
   应该返回API端点信息

3. 如果出现错误，检查：
   - 环境变量是否正确配置
   - MongoDB连接字符串是否正确
   - 查看Vercel部署日志（在项目页面的"Deployments"标签）

### 步骤 2.8：创建管理员账号

后端部署成功后，需要创建一个管理员账号。有两种方法：

**方法一：通过Vercel CLI（推荐）**

1. 安装Vercel CLI：
   ```bash
   npm install -g vercel
   ```

2. 登录Vercel：
   ```bash
   vercel login
   ```

3. 在项目根目录链接到Vercel项目：
   ```bash
   cd d:\【网站设计】\TransGuyane\backend
   vercel link
   ```
   选择您刚创建的项目

4. 设置环境变量（如果还没有）：
   ```bash
   vercel env add MONGODB_URI
   vercel env add JWT_SECRET
   vercel env add NODE_ENV
   vercel env add FRONTEND_URL
   ```

5. 运行创建管理员脚本：
   ```bash
   vercel env pull .env.local
   node scripts/createAdmin.js
   ```

**方法二：在本地运行（需要本地配置环境变量）**

1. 在 `backend` 目录创建 `.env` 文件：
   ```env
   MONGODB_URI=mongodb+srv://用户名:密码@cluster0.xxxxx.mongodb.net/transguyane?retryWrites=true&w=majority
   JWT_SECRET=您的JWT_SECRET
   NODE_ENV=production
   FRONTEND_URL=https://您的GitHub用户名.github.io
   ```

2. 安装依赖：
   ```bash
   cd backend
   npm install
   ```

3. 运行创建管理员脚本：
   ```bash
   node scripts/createAdmin.js
   ```

4. 按照提示输入管理员用户名和密码

---

## 🌐 第三部分：部署前端到 GitHub Pages

### 步骤 3.1：创建 GitHub 仓库（如果还没有）

1. 访问：https://github.com/new
2. 填写仓库信息：
   - **Repository name**: `TransGuyane`
   - **Description**: `TransGuyane 仓库管理系统`
   - **Visibility**: 选择 **Public**（GitHub Pages免费版需要Public仓库）
   - ⚠️ **不要**勾选 "Initialize this repository with a README"
3. 点击 **"Create repository"**

### 步骤 3.2：推送代码到 GitHub

如果项目还没有推送到GitHub，执行以下命令：

```powershell
# 在项目根目录
cd d:\【网站设计】\TransGuyane

# 初始化Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit for deployment"

# 重命名主分支为main（如果还没有）
git branch -M main

# 添加远程仓库（替换为您的GitHub用户名）
git remote add origin https://github.com/您的用户名/TransGuyane.git

# 推送到GitHub
git push -u origin main
```

**提示**：如果提示输入凭证，用户名输入GitHub用户名，密码使用Personal Access Token（不是GitHub密码）。

**如何创建Personal Access Token**：
1. GitHub → 右上角头像 → **Settings**
2. 左侧菜单 → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. 点击 **"Generate new token (classic)"**
5. 填写信息：
   - Note: `Git操作`
   - Expiration: 选择期限（建议90天或No expiration）
   - Scopes: 勾选 **repo**（全部权限）
6. 点击 **"Generate token"**
7. **复制并保存token**（只显示一次）

### 步骤 3.3：配置 GitHub Pages

1. 在GitHub仓库页面，点击 **"Settings"** 标签
2. 在左侧菜单找到 **"Pages"**
3. 在 **"Source"** 下拉菜单中选择 **"GitHub Actions"**
4. 页面会自动保存

### 步骤 3.4：配置 GitHub Actions Secrets

1. 在GitHub仓库页面，点击 **"Settings"** 标签
2. 在左侧菜单找到 **"Secrets and variables"** → **"Actions"**
3. 点击 **"New repository secret"**
4. 添加以下Secret：

   **Secret 1：REACT_APP_API_URL**
   - Name: `REACT_APP_API_URL`
   - Value: `https://您的Vercel项目名.vercel.app/api`
   - 例如：`https://transguyane-backend.vercel.app/api`

5. 点击 **"Add secret"** 保存

### 步骤 3.5：更新 frontend/package.json

检查 `frontend/package.json` 中的 `homepage` 字段是否正确：

```json
{
  "homepage": "https://您的GitHub用户名.github.io/TransGuyane"
}
```

例如：
```json
{
  "homepage": "https://xubo-327.github.io/TransGuyane"
}
```

### 步骤 3.6：触发部署

1. 推送任何更改到main分支，GitHub Actions会自动触发部署：
   ```powershell
   git add .
   git commit -m "Trigger GitHub Pages deployment"
   git push
   ```

2. 或者手动触发：
   - 在GitHub仓库页面，点击 **"Actions"** 标签
   - 如果看到工作流，点击 **"Run workflow"** 按钮

### 步骤 3.7：查看部署状态

1. 在GitHub仓库页面，点击 **"Actions"** 标签
2. 查看部署工作流的状态
3. 等待部署完成（通常需要2-5分钟）
4. 部署成功后，访问：
   ```
   https://您的GitHub用户名.github.io/TransGuyane
   ```

### 步骤 3.8：更新后端 CORS 配置

前端部署成功后，需要更新Vercel中的 `FRONTEND_URL` 环境变量：

1. 在Vercel项目页面，点击 **"Settings"** 标签
2. 在左侧菜单找到 **"Environment Variables"**
3. 找到 `FRONTEND_URL`，点击 **"Edit"**
4. 更新为实际的前端地址：
   ```
   https://您的GitHub用户名.github.io
   ```
5. 点击 **"Save"**
6. 重新部署（会自动触发，或手动点击 **"Redeploy"**）

---

## ✅ 第四部分：验证部署

### 验证步骤

1. **验证前端**
   - 访问：`https://您的GitHub用户名.github.io/TransGuyane`
   - 检查页面是否正常加载
   - 打开浏览器开发者工具（F12），检查控制台是否有错误

2. **验证后端**
   - 访问：`https://您的Vercel项目名.vercel.app/api/health`
   - 应该返回：`{"status":"ok","message":"服务器运行正常"}`

3. **验证数据库连接**
   - 在前端尝试登录
   - 使用创建的管理员账号登录
   - 检查是否能正常访问数据

4. **验证完整功能**
   - 测试用户注册
   - 测试用户登录
   - 测试订单创建、查看等功能
   - 检查所有功能是否正常

---

## 🔧 常见问题解决

### 问题1：前端页面404错误

**原因**：GitHub Pages不支持客户端路由

**解决方案**：
检查 `frontend/src/App.js` 是否使用了 `HashRouter`：

```javascript
import { HashRouter as Router } from 'react-router-dom';
```

如果使用的是 `BrowserRouter`，需要改为 `HashRouter`。

### 问题2：API请求失败（CORS错误）

**原因**：后端CORS配置不正确

**解决方案**：
1. 检查Vercel中的 `FRONTEND_URL` 环境变量是否正确
2. 检查 `backend/server.js` 中的CORS配置是否包含前端URL
3. 重新部署后端

### 问题3：MongoDB连接失败

**原因**：连接字符串错误或网络访问受限

**解决方案**：
1. 检查MongoDB Atlas中的网络访问设置，确保允许所有IP（0.0.0.0/0）
2. 检查连接字符串格式是否正确
3. 检查用户名和密码是否正确
4. 如果密码包含特殊字符，确保已进行URL编码

### 问题4：GitHub Actions部署失败

**原因**：构建错误或配置问题

**解决方案**：
1. 检查GitHub Actions日志，查看具体错误信息
2. 确保 `REACT_APP_API_URL` Secret已正确配置
3. 确保 `frontend/package.json` 中的 `homepage` 字段正确
4. 检查前端代码是否有语法错误

### 问题5：Vercel部署失败

**原因**：环境变量缺失或代码错误

**解决方案**：
1. 检查Vercel部署日志，查看具体错误
2. 确保所有必需的环境变量都已配置
3. 检查 `backend/vercel.json` 配置是否正确
4. 检查 `backend/api/index.js` 是否存在且正确

---

## 📝 更新代码流程

### 更新前端

1. 修改前端代码
2. 提交并推送：
   ```powershell
   git add .
   git commit -m "Update frontend"
   git push
   ```
3. GitHub Actions会自动触发部署（约2-5分钟）

### 更新后端

1. 修改后端代码
2. 提交并推送：
   ```powershell
   git add .
   git commit -m "Update backend"
   git push
   ```
3. Vercel会自动触发部署（约1-3分钟）

### 更新环境变量

**Vercel环境变量**：
1. 在Vercel项目设置中更新环境变量
2. 点击 **"Redeploy"** 重新部署

**GitHub Secrets**：
1. 在GitHub仓库设置中更新Secrets
2. 手动触发Actions部署或推送代码

---

## 🔒 安全建议

1. **保护敏感信息**
   - 不要在前端代码中暴露API密钥
   - 使用环境变量存储敏感配置
   - 不要将 `.env` 文件提交到Git

2. **数据库安全**
   - 使用强密码
   - 定期轮换JWT Secret
   - 考虑限制MongoDB Atlas的网络访问（仅允许Vercel IP）

3. **HTTPS**
   - GitHub Pages和Vercel都默认使用HTTPS
   - 确保所有API调用都使用HTTPS

---

## 📊 部署架构图

```
┌─────────────────┐
│  用户浏览器      │
└────────┬────────┘
         │
         │ HTTPS
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌──────────┐  ┌──────────┐
│ GitHub   │  │  Vercel  │
│ Pages    │  │  Backend │
│ Frontend │  │  API     │
└──────────┘  └────┬─────┘
                   │
                   │ HTTPS
                   │
              ┌────┴─────┐
              │ MongoDB  │
              │ Atlas    │
              └──────────┘
```

---

## 🎉 完成！

恭喜！您已成功部署完整的系统：

- ✅ 前端：GitHub Pages（免费、自动HTTPS、全球CDN）
- ✅ 后端：Vercel（免费、Serverless、自动部署）
- ✅ 数据库：MongoDB Atlas（免费、云端、自动备份）

**总成本：$0/月** 🆓

---

## 📚 相关文档

- [MongoDB Atlas配置指南](./MongoDB%20Atlas配置指南.md)
- [Vercel部署指南](./docs/deployment/VERCEL_DEPLOYMENT.md)
- [GitHub Pages部署指南](./docs/deployment/GITHUB_PAGES_DEPLOYMENT.md)

---

**最后更新**：2026年1月

**需要帮助？** 如果遇到任何问题，请查看常见问题部分或查看相关文档。
