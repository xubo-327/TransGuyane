# 快速部署指南

> 完整的部署文档请参考：[完整部署方案-重新开始.md](./完整部署方案-重新开始.md)

## 🎯 三个步骤概览

1. **配置 MongoDB Atlas** - 云数据库
2. **部署后端到 Vercel** - Serverless API
3. **部署前端到 GitHub Pages** - 静态网站

---

## 第一步：MongoDB Atlas（5分钟）

1. 注册：https://www.mongodb.com/cloud/atlas/register
2. 创建免费集群（M0）
3. 创建数据库用户（记住用户名密码）
4. 配置网络访问：允许所有IP `0.0.0.0/0`
5. 获取连接字符串，格式：
   ```
   mongodb+srv://用户名:密码@cluster0.xxxxx.mongodb.net/transguyane?retryWrites=true&w=majority
   ```
6. 生成 JWT Secret（至少32字符）

---

## 第二步：Vercel 后端（10分钟）

1. 注册：https://vercel.com/signup（使用GitHub登录）
2. 导入项目 → 选择仓库 → Root Directory: `backend`
3. 添加环境变量：
   - `MONGODB_URI` = 您的连接字符串
   - `JWT_SECRET` = 您的JWT密钥
   - `NODE_ENV` = `production`
   - `FRONTEND_URL` = `https://您的用户名.github.io`（稍后更新）
4. 点击 Deploy
5. 等待部署完成，复制后端URL：`https://项目名.vercel.app/api`
6. 验证：访问 `https://项目名.vercel.app/api/health`

---

## 第三步：GitHub Pages 前端（10分钟）

1. 推送代码到GitHub（如果还没有）：
   ```powershell
   git add .
   git commit -m "Initial commit"
   git push -u origin main
   ```
2. GitHub仓库 → Settings → Pages → Source: **GitHub Actions**
3. Settings → Secrets → Actions → 添加Secret：
   - `REACT_APP_API_URL` = `https://您的Vercel项目名.vercel.app/api`
4. 检查 `frontend/package.json` 中的 `homepage`：
   ```json
   {
     "homepage": "https://您的用户名.github.io/TransGuyane"
   }
   ```
5. 推送代码触发部署：
   ```powershell
   git push
   ```
6. Actions标签查看部署状态
7. 部署完成后访问：`https://您的用户名.github.io/TransGuyane`
8. 更新 Vercel 的 `FRONTEND_URL` 为实际前端地址，重新部署

---

## 🎯 关键配置

### MongoDB 连接字符串格式
```
mongodb+srv://用户名:密码@cluster0.xxxxx.mongodb.net/transguyane?retryWrites=true&w=majority
```
⚠️ 注意：密码中的特殊字符需要URL编码

### Vercel 环境变量
- `MONGODB_URI` - 完整连接字符串
- `JWT_SECRET` - 至少32字符
- `NODE_ENV` - `production`
- `FRONTEND_URL` - 前端完整URL（无末尾斜杠）

### GitHub Secrets
- `REACT_APP_API_URL` - 后端API地址（末尾包含 `/api`）

### package.json homepage
```json
{
  "homepage": "https://您的用户名.github.io/TransGuyane"
}
```

---

## 🐛 常见问题速查

**前端404** → 确保使用 `HashRouter`

**CORS错误** → 检查Vercel的 `FRONTEND_URL` 是否正确

**MongoDB连接失败** → 检查网络访问设置（允许所有IP）

**构建失败** → 检查GitHub Actions日志，确保Secrets配置正确

---

## ✅ 验证清单

- [ ] 前端正常加载：`https://用户名.github.io/TransGuyane`
- [ ] 后端健康检查：`https://项目名.vercel.app/api/health`
- [ ] 可以登录/注册
- [ ] 可以创建订单

---

## 📞 需要帮助？

查看完整文档：[完整部署方案-重新开始.md](./完整部署方案-重新开始.md)
