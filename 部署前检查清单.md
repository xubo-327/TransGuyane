# 部署前检查清单

在开始部署之前，请完成以下配置：

## 📝 第一步：更新配置文件

### 1. 更新 `frontend/package.json`

找到 `homepage` 字段，替换为您的 GitHub Pages URL：

```json
{
  "homepage": "https://您的GitHub用户名.github.io/TransGuyane"
}
```

**示例**：
```json
{
  "homepage": "https://zhangsan.github.io/TransGuyane"
}
```

### 2. 创建 `frontend/.env.production`

创建文件并添加后端 API URL：

```env
REACT_APP_API_URL=https://您的Vercel项目名.vercel.app/api
```

**示例**：
```env
REACT_APP_API_URL=https://transguyane-backend.vercel.app/api
```

⚠️ **注意**：先部署后端到 Vercel，获取 API URL 后再配置前端。

---

## 🔑 第二步：准备密钥和配置

### MongoDB Atlas 连接字符串

格式：
```
mongodb+srv://用户名:密码@集群地址.mongodb.net/数据库名?retryWrites=true&w=majority
```

### JWT Secret

生成一个至少32字符的随机字符串，例如：
```bash
# 在命令行运行（Windows PowerShell）
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})
```

或在 Linux/Mac：
```bash
openssl rand -base64 32
```

---

## ✅ 第三步：部署顺序

1. **先部署后端到 Vercel**
   - 获取后端 API URL
   - 配置前端 `.env.production` 文件

2. **再部署前端到 GitHub Pages**
   - 推送代码到 GitHub
   - GitHub Actions 自动部署

3. **最后配置 Vercel CORS**
   - 在 Vercel 环境变量中添加 `FRONTEND_URL`
   - 重新部署后端

---

## 📋 配置检查

部署前确认：

- [ ] `frontend/package.json` 中的 `homepage` 已更新
- [ ] `frontend/.env.production` 已创建（可选，也可以用 GitHub Secrets）
- [ ] MongoDB Atlas 连接字符串已准备好
- [ ] JWT Secret 已准备好
- [ ] GitHub 仓库已创建（Public）
- [ ] GitHub 用户名已知

---

## 🚀 开始部署

完成检查后，按照 [Vercel后端+GitHub Pages前端部署方案.md](./Vercel后端+GitHub%20Pages前端部署方案.md) 进行部署。
