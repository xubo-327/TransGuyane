# 项目结构说明

## 📁 目录结构

```
货柜服务网站/
├── frontend/                 # 前端应用（React）
│   ├── public/              # 静态资源
│   │   ├── index.html       # HTML模板
│   │   └── logo.jpg         # Logo图片
│   ├── src/                 # 源代码
│   │   ├── components/      # 公共组件
│   │   ├── contexts/        # React Context
│   │   ├── layouts/         # 布局组件
│   │   ├── pages/           # 页面组件
│   │   │   ├── admin/       # 管理端页面
│   │   │   ├── user/        # 用户端页面
│   │   │   ├── Login.js     # 登录页面
│   │   │   └── Register.js  # 注册页面
│   │   ├── services/        # API服务
│   │   ├── utils/           # 工具函数
│   │   ├── App.js           # 根组件
│   │   └── index.js         # 入口文件
│   ├── Dockerfile           # Docker配置
│   ├── nginx.conf           # Nginx配置
│   └── package.json         # 依赖配置
│
├── backend/                  # 后端应用（Node.js + Express）
│   ├── models/              # 数据模型（Mongoose）
│   ├── routes/               # API路由
│   ├── middleware/           # 中间件
│   ├── services/             # 业务服务
│   ├── scripts/              # 脚本工具
│   ├── server.js             # 服务器入口
│   ├── Dockerfile           # Docker配置
│   └── package.json         # 依赖配置
│
├── deploy-package/           # 部署包（不提交到Git）
│   ├── frontend/            # 前端构建产物
│   └── backend/             # 后端部署文件
│
├── docs/                     # 项目文档
│   ├── deployment/          # 部署文档
│   ├── api/                 # API文档
│   └── guides/              # 使用指南
│
├── scripts/                  # 开发脚本（可选）
│   ├── dev/                 # 开发环境脚本
│   └── deploy/              # 部署脚本
│
├── .gitignore               # Git忽略配置
├── README.md                # 项目说明
├── PROJECT_STRUCTURE.md     # 项目结构说明（本文件）
└── package.json             # 根目录配置
```

## 📋 目录说明

### Frontend（前端）

- **public/**: 静态资源文件，构建时直接复制到输出目录
- **src/components/**: 可复用的React组件
- **src/contexts/**: React Context，用于全局状态管理（如认证状态）
- **src/layouts/**: 页面布局组件（用户端布局、管理端布局）
- **src/pages/**: 页面组件，按功能模块组织
- **src/services/**: API服务层，封装所有后端API调用
- **src/utils/**: 工具函数（如快递公司识别）

### Backend（后端）

- **models/**: Mongoose数据模型定义
- **routes/**: Express路由定义，按功能模块划分
- **middleware/**: 中间件（认证、权限检查等）
- **services/**: 业务逻辑服务层
- **scripts/**: 工具脚本（创建管理员、测试连接等）

### Docs（文档）

- **deployment/**: 部署相关文档
- **api/**: API接口文档
- **guides/**: 使用指南和教程

## 🔧 配置文件说明

### 环境变量

#### Frontend (.env)
```env
REACT_APP_API_URL=http://localhost:5000/api
```

#### Backend (.env)
```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/warehouse_management
JWT_SECRET=your_secret_key_here
WECHAT_APPID=your_wechat_appid
WECHAT_SECRET=your_wechat_secret
NODE_ENV=development
```

## 📦 依赖管理

### 前端依赖
- React 18 - UI框架
- React Router v6 - 路由管理
- Ant Design 5 - UI组件库
- Axios - HTTP客户端
- Day.js - 日期处理

### 后端依赖
- Express - Web框架
- Mongoose - MongoDB ODM
- JWT - 身份认证
- Bcrypt - 密码加密
- Express Validator - 数据验证
- XLSX - Excel处理

## 🚀 开发流程

1. **安装依赖**: `npm run install-all`
2. **配置环境变量**: 创建 `.env` 文件
3. **启动开发服务器**: `npm run dev`
4. **访问应用**: http://localhost:3000

## 📝 代码规范

- 使用ES6+语法
- 组件使用函数式组件和Hooks
- API调用统一使用services层
- 错误处理统一格式
- 代码注释清晰

## 🔒 安全注意事项

- 生产环境必须修改JWT_SECRET
- 环境变量文件不要提交到Git
- 使用HTTPS部署
- 定期更新依赖包
